Apply a fully specified, repeatable edit with no new product decisions.

Change only the named scope.
Follow the supplied acceptance criteria exactly.
Run the smallest relevant checks and report the actual files changed.
Stop if the task needs design judgment.
